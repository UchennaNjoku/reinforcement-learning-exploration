# MARL Comms Research

Initial workspace for the multi-agent communication project.

Current phase:

1. Freeze the problem in `specs.md`
2. Stand up `pettingzoo.sisl.pursuit_v4`
3. Inspect raw observations and actions

Use the repo venv:

```bash
source .venv/bin/activate
pip install -r marl-comms-research/requirements.txt
python marl-comms-research/scripts/inspect_pursuit.py
```

